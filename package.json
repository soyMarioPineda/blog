{
  "name": "mi-blog",
  "version": "1.0.0",
  "description": "Blog personal — HTML/CSS puro con Markdown",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js watch",
    "dev":   "npx live-server --open=index.html"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked":      "^12.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
